---
const { href } = Astro.props;
const isActive = Astro.url.pathname === href;
---

<a
  href={href}
  @click="sidebarIsOpen = false"
  transition:name="fade"
  class={`block px-3 py-2 rounded ${isActive ? "bg-blue-500 text-white" : "hover:bg-blue-100"}`}
>
  <slot />
</a>
